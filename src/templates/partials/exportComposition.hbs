{{#ifdef description deprecated}}
/**
{{#if description}}
 * {{{escapeComment description}}}
{{/if}}
{{#if deprecated}}
 * @deprecated
{{/if}}
 */
{{/ifdef}}
export type {{{getModuleType name}}} = {{>type parent=name}};
{{#if enums}}
{{#unless @root.useUnionTypes}}

export namespace {{{getModuleType name}}} {

	{{#each enums}}
	{{#ifdef description deprecated}}
	/**
	{{#if description}}
	 * {{{escapeComment description}}}
	{{/if}}
	{{#if deprecated}}
	 * @deprecated
	{{/if}}
	 */
	{{/ifdef}}
	export enum {{{getModuleType name}}} {
		{{#each enum}}
		{{{name}}} = {{{value}}},
		{{/each}}
	}

	{{/each}}

}
{{/unless}}
{{/if}}
